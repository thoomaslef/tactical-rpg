{
  "name": "tactical-rpg",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && vercel --prod --yes",
    "remove-bg": "node scripts/remove-bg.cjs",
    "remove-bg:watch": "node scripts/remove-bg.cjs --watch"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "jimp": "^1.6.1",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
